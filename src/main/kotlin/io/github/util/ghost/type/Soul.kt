package io.github.util.ghost.type

import io.github.util.ghost.Ghost
import io.github.util.room.Room
import io.github.util.room.RoomSystem
import net.minecraft.world.World

class Soul(world: World, roomSystem: RoomSystem, initRoom: Room) : Ghost(world, roomSystem, initRoom)
